import React from 'react';

function Search(props) {
    return (
        <section className="searchbox-swap">
            <input type="text" className="searchbox" placeholder="Search for a movies ..." />
        </section>
    );
}

export default Search;